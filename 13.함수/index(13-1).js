// 함수 선언문
function sum(param1, param2) {
  return param1 + param2;
}
console.log(sum(1, 2)); // 3

const obj = {
  // 메서드
  prop: function () {},
};

// 생성자 함수
function MyObj() {}

// 화살표 함수 (ES2015+, ES6)
const arrowFunc = () => {};
